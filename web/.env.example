# Backend origin. Set this to the backend subdomain for separate Hostinger apps.
# In local development, backend runs on localhost:4000.
API_URL=http://localhost:4000
NEXT_PUBLIC_API_URL=http://localhost:4000
