{
  "name": "employee-work-tracker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "main": "server.js",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "clean": "node -e \"require('fs').rmSync('web/out',{recursive:true,force:true});require('fs').rmSync('server/dist',{recursive:true,force:true});require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "npm run build -w server && npm run build -w web && node scripts/package-hostinger.mjs",
    "start": "node server.js",
    "lint": "npm run lint -w server && npm run lint -w web",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web",
    "setup": "npm run setup -w server",
    "backup": "npm run backup -w server",
    "backup:list": "npm run backup:list -w server",
    "test": "npm test -w server && npm test -w web"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "file:vendor/better-sqlite3"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
